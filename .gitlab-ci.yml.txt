# We don't specify any image since the shell executor will use the system's environment

# Define the stages
stages:
  - test  # You can add more stages later (e.g., build, deploy)

# Define the job in the test stage
test-job:
  stage: test
  script:
    - echo "Installing dependencies..."
    - pip install jupyter nbconvert  # Install Jupyter to execute notebooks
    - echo "Running the notebook..."
    - jupyter nbconvert --to notebook --execute Project_ECG.ipynb --output result.ipynb  # Execute the notebook
